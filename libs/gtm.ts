export const GTM_ID = process.env.NEXT_PUBLIC_GTM_ID;

// Disable GTM. Let's try Vercel Analytics!

// export const GTM_AVAILABLE = process.env.NODE_ENV === 'production';
export const GTM_AVAILABLE = false;
